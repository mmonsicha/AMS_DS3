export default function DashboardPage() {
  return (
    <div style={{ alignItems: "center", display: "flex", fontFamily: "DB HeaventRounded, sans-serif",
      fontSize: "var(--text-h3)", justifyContent: "center", minHeight: "100vh" }}>
      🎉 เข้าสู่ระบบสำเร็จ — Dashboard (placeholder)
    </div>
  );
}
